[project]
name = "bruce-sdk-python"
version = "0.1.0"
description = "Bruce OpenTelemetry SDK for Python agents"
authors = [
    {name = "Bruce Maintainers"}
]
dependencies = [
    "opentelemetry-api",
    "opentelemetry-sdk",
]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
